<div class="block" id="block-text">
  <div class="content">
    <h2 class="title"><%= I18n.t :statistics -%></h2>
    <div class="inner">
      <p class="first">

	<% if @hosts.length == 0 %>
	<%= I18n.t :nohost -%>
	<% else %>
	<b><%= @hosts.length %></b> hosts
	<% end %>
      </p>

    </div>
  </div>
</div>
